import React from 'react';

const RegisterComponent = (props) => {
    return (
        <>
            <div>This is the register component div</div>
        </>
    );
};

export default RegisterComponent;